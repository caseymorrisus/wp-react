const Home = props => (
  <DocumentTitle title={WPReact.createTitle('Home')}>
    <div className="home">
      <h2>Home Page</h2>
      <p>This is the front page.</p>
    </div>
  </DocumentTitle>
)

export default Home